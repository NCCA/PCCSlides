[project]
name = "bouncingball"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jon Macey", email = "jmacey@bournemouth.ac.uk" }
]
requires-python = ">=3.13"
dependencies = [
    "nccapy>=0.1.7",
]

[project.scripts]
bouncingball = "bouncingball:main"

[build-system]
requires = ["uv_build>=0.8.14,<0.9.0"]
build-backend = "uv_build"
